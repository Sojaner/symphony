---
type: llm
focus: last_message
---

Pass only if the complete report consistently describes a planned dry run: strongest/high read-only assessment finishes before a separate balanced/medium execution lead, with exactly one medium mode. Fail on any contradictory prose, even if all required record lines also appear: assessor implementation or delegation, reusing the assessor as execution lead, strongest/high medium execution, another mode selection, or claims that agents actually ran. Planned completion records must say planned and use `not exposed by host` for tokens and duration. This is evidence of a plan only, not real dispatch or lifecycle acceptance.
